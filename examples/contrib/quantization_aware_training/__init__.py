"""Quantization-aware training example.

This previously re-exported `from .layers import *`, but no `layers` module
exists here or anywhere else in the example, so importing the package raised
ImportError. The example's modules (models, utils, datasets) are imported
directly by its scripts.
"""
